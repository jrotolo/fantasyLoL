<%= provide(:title, @user.name) %>
<div class="row">
	<aside class="span4">
		<section>
			<h1>
				<%= gravatar_for @user %>
				<%= @user.name %>
			</h1>
		</section>
	</aside>	
</div>

<div class="row">
	<div class="span3 offset10">
		<%= button_to "Create a Team", new_team_path, :method => "get", class: "btn btn btn-primary" %>
	</div>
</div>